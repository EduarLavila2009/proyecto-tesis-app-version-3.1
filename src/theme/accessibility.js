/**
 * Área táctil ampliada (~44dp efectivos con hitSlop 10 en controles pequeños).
 * WCAG 2.5.5 / práctica recomendada Apple HIG.
 */
export const hitSlopComfortable = {
  top: 10,
  bottom: 10,
  left: 10,
  right: 10,
};
